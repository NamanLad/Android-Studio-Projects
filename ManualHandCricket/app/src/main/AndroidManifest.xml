<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manualhandcricket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/manualhandcricket_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/manualhandcricket_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BowlFirst"></activity>
        <activity android:name=".BatFirst" />
        <activity android:name=".Toss">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserBowling" />
        <activity android:name=".MainActivity" >


        </activity>

    </application>

</manifest>